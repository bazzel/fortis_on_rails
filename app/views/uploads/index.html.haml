%h1 Listing uploads

%table
  %tr
    %th Period start
    %th Period end
    %th Balance start
    %th Balance end
    %th
    %th
    %th

  - @uploads.each do |upload|
    %tr
      %td= upload.period_start
      %td= upload.period_end
      %td= upload.balance_start
      %td= upload.balance_end
      %td= link_to 'Show', upload
      %td= link_to 'Edit', edit_upload_path(upload)
      %td= link_to 'Destroy', upload, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New upload', new_upload_path
